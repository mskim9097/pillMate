<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', { title: 'Log In – DailyDose' }) %>

    <body>
        <%- include('partials/header') %>

            <main class="page-center container py-5 text-center">
                <h1 class="mb-2 text-success">Log In</h1>
                <p class="text-muted">Welcome back! Please enter your account details.</p>

                <% if (error) { %>
                    <p class="error text-danger fw-semibold">
                        <%= error %>
                    </p>
                    <% } %>

                        <form class="d-inline-block text-start bg-white p-4 rounded-3 shadow-sm" method="post"
                            action="/login">
                            <label>Email:</label>
                            <input class="form-control" type="email" name="email" required />

                            <label class="mt-3">Password:</label>
                            <input class="form-control" type="password" name="password" required />

                            <button class="btn btn-success w-100 mt-3" type="submit">Log In</button>
                        </form>

                        <p class="auth-switch mt-3">
                            Don’t have an account? <a href="/signup">Sign up</a>
                        </p>
            </main>

            <%- include('partials/footer') %>
                <%- include('partials/footer-scripts') %>
    </body>

</html>