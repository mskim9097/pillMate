<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>DailyDose</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-light d-flex flex-column min-vh-100">
    <main class="container text-center py-5">
        <div class="fs-1">💊</div>
        <h1 class="display-5 fw-bold text-success mb-2">Welcome to DailyDose</h1>

        <% if (authenticated) { %>
            <p class="text-secondary">Hello, <strong>
                    <%= name %>
                </strong>! You’re logged in and ready to stay healthy.</p>
            <div class="d-flex gap-2 justify-content-center mt-3">
                <a href="/main" class="btn btn-success">Go to Dashboard</a>
                <form action="/logout" method="POST">
                    <button class="btn btn-outline-danger">Log Out</button>
                </form>
            </div>
            <% } else { %>
                <p class="text-secondary">Your personal supplement & vitamin reminder assistant.</p>
                <div class="d-flex gap-2 justify-content-center mt-3">
                    <a href="/login" class="btn btn-success">Log In</a>
                    <a href="/signup" class="btn btn-outline-success">Sign Up</a>
                </div>
                <% } %>

                    <footer class="mt-5 text-muted">
                        🌱 DailyDose &copy; <%= new Date().getFullYear() %> — Stay consistent, stay healthy!
                    </footer>
    </main>
</body>

</html>