<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', { title: 'PillMate' }) %>

    <body>
        <%- include('partials/header') %>

            <main class="landing container text-center py-5">
                <div class="emoji fs-1">
                    <img src="/favicon.png" alt="PillMate logo" width="64" height="64" class="pill" />
                </div>
                <h1 class="landing-title">Welcome to PillMate</h1>

                <% if (authenticated) { %>
                    <p>Hello, <strong>
                            <%= name %>
                        </strong>! You’re logged in and ready to stay healthy.</p>
                    <a href="/main" class="btn btn-success">Go to Dashboard</a>
                    <form class="d-inline-block ms-2" action="/logout" method="POST">
                        <button class="btn btn-danger">Log Out</button>
                    </form>
                    <% } else { %>
                        <p>Your personal supplement & vitamin reminder assistant.</p>
                        <div class="btn-group">
                            <a href="/login" class="btn btn-success">Log In</a>
                            <a href="/signup" class="btn btn-outline-success">Sign Up</a>
                        </div>
                        <% } %>

                            <footer class="mt-5 text-muted">
                                <p>🌱 PillMate &copy; <%= new Date().getFullYear() %> — Stay consistent, stay healthy!
                                </p>
                            </footer>
            </main>

            <%- include('partials/footer-scripts') %>
    </body>

</html>