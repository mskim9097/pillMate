<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>DailyDose</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css" />
</head>

<body class="bg-light">
    <main class="container py-5">
        <div class="text-center">
            <div class="pill-icon mx-auto mb-3">💊</div>
            <h1 class="fw-bold text-success mb-2">Welcome to DailyDose</h1>

            <% if (authenticated) { %>
                <p class="text-muted">Hello, <strong>
                        <%= name %>
                    </strong>! You’re logged in.</p>
                <div class="d-flex gap-2 justify-content-center">
                    <a href="/main" class="btn btn-success">Go to Dashboard</a>
                    <form action="/logout" method="POST">
                        <button class="btn btn-danger">Log Out</button>
                    </form>
                </div>
                <% } else { %>
                    <p class="text-muted">Your personal supplement & vitamin reminder assistant.</p>
                    <div class="d-flex gap-2 justify-content-center">
                        <a href="/login" class="btn btn-success">Log In</a>
                        <a href="/signup" class="btn btn-outline-success">Sign Up</a>
                    </div>
                    <% } %>

                        <footer class="mt-5 text-muted">
                            🌱 DailyDose &copy; <%= new Date().getFullYear() %>
                        </footer>
        </div>
    </main>
</body>

</html>